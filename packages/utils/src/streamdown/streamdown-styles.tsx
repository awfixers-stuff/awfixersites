"use client";

import "streamdown/styles.css";

/**
 * Loads Streamdown's default stylesheet. Mount once in the root layout
 * when rendering streamed markdown with `@awfixersites/utils/streamdown`.
 */
function StreamdownStyles() {
  return null;
}

export { StreamdownStyles };
